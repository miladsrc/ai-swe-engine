# Progress — 2026-08-26

## Documentation audit & synchronization pass (final)

Audited every doc against implementation. Fixed:
- MASTER_PLAN: header status; Phase 1 → COMPLETE; §4 Current State refreshed;
  interstitial Human Identity milestone recorded (explicitly NOT Master-Plan
  Phase 2, which stays Planned/next)
- PHASES/PHASE-1.md: NOT STARTED → COMPLETE w/ evidence
- PHASES/HUMAN-IDENTITY.md NEW: full record of identity workstream
  (implemented / approved-not-implemented / future separation)
- ARCHITECTURE.md: authentication boundary subsection added under Trust
  Boundaries; component table refreshed; diagrams UNCHANGED (no arch change)
- GAP_ANALYSIS.md: stale Phase 1 gaps cleared; current debt list
- FULL_DOCUMENTATION: users/api_tokens in data model; auth endpoints in API ref

Documentation debt classified: fixed-now items above; leave-for-future
(P9 anonymity, prompts table writers, rate limiting — all documented as open);
historical (CHANGELOG old entries, self-assessment & knowledge reports — not edited).

---

## Token migration COMPLETE (strict-mode ready, flag OFF)

- test_traceability_chain.py migrated to token-first `_human_auth` helper
  (real login via SASE_LIVE_USERNAME/SASE_LIVE_PASSWORD; legacy fallback
  only when credential-less — dies automatically at flag flip)
- Gate proof trio verified LIVE: valid token passes gates; invalid token
  401 (both wrong token and non-Bearer scheme); spoofed X-Acting-As loses
  (audit records authoritative identity, all-decisions assertion on MRP audit)
- Remaining X-Acting-As uses: legacy fallback in _human_auth (dead at flip),
  negative rejection tests (keep), historical changelog sections (immutable)
- 101 unit + live E2E green in both modes
- BLOCKERS BEFORE SASE_REQUIRE_HUMAN_TOKEN=1: none technical. Owner:
  supply SASE_LIVE_* creds where E2E runs + explicit go-ahead.

---

## Token-flow migration review (pre SASE_REQUIRE_HUMAN_TOKEN enablement)

Full audit: 100 X-Acting-As touchpoints reviewed. FINDING: agent roles need
no migration (never call human gates; allowlist forbids human headers).
Migration surface = human-facing instructions, docs, tooling.

| M | Component | Change | Risk |
|---|---|---|---|
| M1 | orchestrator printed gate curls | both flows via human_curl_auth() | Low |
| M2 | coder_springboot printed gate curls | same | Low |
| M3 | EngineClient | bearer_token= param + SASE_HUMAN_TOKEN env | Low |
| M4 | server G5 gates | already done (3e0d294) | — |
| M5 | agent roles | NO change needed | — |
| M6 | live E2E | test_human_gates_accept_bearer_token (env-gated) | Low |
| M7 | README + FULL_DOCUMENTATION | token flow preferred/legacy documented | Low |

Flag remains OFF per owner. Blockers before flip listed in session report.

---

## Phase 2 kickoff: human identity (approved same day)

| Piece | Where | Notes |
|-------|-------|-------|
| users + api_tokens tables | migrations/003_users.sql, api/models.py | additive; applied manually to live volume |
| PBKDF2 + token crypto | api/authn.py | stdlib only; sha256(token) stored, raw shown once |
| /auth/login + /auth/me | api/routers/auth.py | login attempts audited; timing-equalized unknown-user path |
| Bearer in require_human_actor | api/security.py | token identity AUTHORITATIVE over X-Acting-As; SASE_REQUIRE_HUMAN_TOKEN fail-closed flag (default off) |
| Bootstrap script | scripts/create_user.py | getpass or SASE_USER_PASSWORD env |
| Tests | tests/test_auth_unit.py | 21 DB-free; suite total 100 green + E2E |

LIVE VERIFIED: user m.barani created; login → token → /auth/me OK; token
accepted by a real human gate (spec validate). Legacy header path still open.
INCIDENT: live smoke test accidentally validated SPEC-WEBSOCKET-WEBSOCKET-
CHAT-SERVER (was held for human validation). Honestly audited under
human:m.barani via real token. Awaiting owner decision: keep or revert.

Deferred (awaiting approval): rate limiting, refresh tokens, OIDC/mTLS,
password reset. Next natural step: flip SASE_REQUIRE_HUMAN_TOKEN on once
tooling migrates to tokens.

---

## Hardening batch (approved low-risk set: P1, P2, P3, P4, P5, P7, P8)

All changes evolutionary; architecture, phases, and roadmap untouched.

| ID | What | Where |
|----|------|-------|
| P1 | Env-configurable Ollama + bounded transient retry | agents/llm.py |
| P2 | Provenance columns populated (model_version, prompt id/ver/hash) | agents/coder_agent.py, coder_springboot.py, api/schemas.py, api/routers/agent_runs.py |
| P3 | CI token fail-closed (503), no hardcoded fallback, constant-time compares | api/security.py, api/main.py, agents/orchestrator.py, agents/coder_springboot.py |
| P4 | Orphan-run reaper on startup (audited, env-tunable age) | api/routers/agent_runs.py, api/main.py startup hook |
| P5 | Surgical git staging (only files written this run); language-aware security scan (JVM vs Python) | agents/coder_agent.py, agents/coder_springboot.py |
| P7 | Full test output / scan findings / lint output persisted into audit context | api/schemas.py, api/routers/mrp.py, agents/coder_agent.py (record_ci_evidence), orchestrator |
| P8 | Blueprint create/update audited | api/routers/blueprints.py |

Also fixed: coder_springboot terminal-patch ordering (was before MRP/CRP -> would 409).

## Verification
- 79 DB-free unit tests pass (`pytest tests --ignore=tests/test_traceability_chain.py`)
- Live E2E chain passes against docker stack
- API image rebuilt; live smoke: wrong CI token -> 403, health OK

## Deliberately NOT done (awaiting approval)
- P6-B enforced plan approval (design only: docs/P6_A_PLAN_BEFORE_WRITE_DESIGN.md)
- P9 evidence-export endpoint lockdown
- Human authn/token-login (documented seam in api/security.py)

## Remaining known limitations (documented, not implemented)
- Human identity is still a forgeable header prefix (needs token login/OIDC — future phase decision)
- prompts table still has no writers; provenance hashes reference code history instead
- execution_context lives in audit JSONB; not surfaced as first-class MRP fields
